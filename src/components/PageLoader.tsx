import { Mountain } from "lucide-react";

export default function PageLoader() {
  return (
    <div className="grid min-h-screen place-items-center bg-sand-50">
      <Mountain className="h-10 w-10 animate-pulse text-pine-400" />
    </div>
  );
}
